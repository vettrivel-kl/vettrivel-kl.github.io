---
sidebar_position: 2
title: Missing Values
description: Why a value is missing decides what you may do about it — MCAR, MAR and MNAR, what mean imputation costs a distribution, when a smarter imputer pays, and the fit/transform contract that keeps it honest.
tags: [machine-learning, data-preprocessing, scikit-learn, pandas]
toc_max_heading_level: 3
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Heading from '@theme/Heading';
import React from 'react';

# Missing Values

> **Topic -** `SimpleImputer(strategy="mean")` takes two lines and always executes without raising an error or warning, returning a fully populated matrix. This chapter examines what those two lines actually do to your data distributions, when mean substitution is statistically legitimate, and why **why a value is missing matters far more than which function you use to fill it**.

Filling missing entries is not just data formatting — it is an act of statistical estimation. Imputing improperly introduces silent biases and artificially narrows variance.

---

## 1. Rubin's Missingness Taxonomy: MCAR vs. MAR vs. MNAR

In mathematical statistics, missing data mechanisms are structured into three distinct regimes defined by Donald Rubin (1976). The mechanism determines whether missing information can be recovered at all:

| Mechanism | Formal Definition | Real-World Example | Can Imputation Recover It? |
|---|---|---|---|
| **MCAR** (Missing Completely at Random) | Missingness is strictly independent of both observed and unobserved variables: $P(M \mid Y_{obs}, Y_{mis}) = P(M)$. | A laboratory test tube drops and shatters randomly on the floor. | **Yes.** Surviving rows form an unbiased representative sample. |
| **MAR** (Missing at Random) | Missingness depends on **other observed features**, but not the missing value itself: $P(M \mid Y_{obs}, Y_{mis}) = P(M \mid Y_{obs})$. | Younger survey respondents are less likely to report salary, but Age is recorded in the table. | **Yes.** Multivariate algorithms (such as KNN or MICE) can reconstruct it using observed columns. |
| **MNAR** (Missing Not at Random) | Missingness depends directly on **the unobserved missing value itself**: $P(M \mid Y_{obs}, Y_{mis}) \neq P(M \mid Y_{obs})$. | High-income earners refuse to disclose their salaries on tax forms due to wealth privacy. | **No.** The missing data is systematically biased; no statistical imputer can recover the true mean. |

---

export const MissingDataMechanismQuiz = () => {
  const [selected, setSelected] = React.useState(null);
  return (
    <div style={{ padding: '20px', border: '1px solid var(--ifm-color-emphasis-200)', borderRadius: '8px', backgroundColor: 'rgba(148, 163, 184, 0.05)', margin: '20px 0' }}>
      <h4 style={{ margin: '0 0 10px 0' }}>Interactive Checkpoint: Classifying Data Gaps</h4>
      <p style={{ margin: '0 0 15px 0' }}><b>Scenario:</b> A clinical sensor stops recording blood pressure metrics whenever a patient's temperature spikes above 104°F (and temperature is recorded separately in the same table). What is the missingness mechanism of the blood pressure column?</p>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
        <button onClick={() => setSelected('mcar')} style={{ padding: '10px 14px', textAlign: 'left', borderRadius: '4px', border: '1px solid #3182ce', backgroundColor: selected === 'mcar' ? '#e53e3e' : 'transparent', color: selected === 'mcar' ? '#fff' : 'var(--ifm-font-color-base)', cursor: 'pointer', transition: '0.2s' }}>
          Option A: MCAR (Missing Completely at Random)
        </button>
        <button onClick={() => setSelected('mar')} style={{ padding: '10px 14px', textAlign: 'left', borderRadius: '4px', border: '1px solid #3182ce', backgroundColor: selected === 'mar' ? '#38a169' : 'transparent', color: selected === 'mar' ? '#fff' : 'var(--ifm-font-color-base)', cursor: 'pointer', transition: '0.2s' }}>
          Option B: MAR (Missing at Random)
        </button>
        <button onClick={() => setSelected('mnar')} style={{ padding: '10px 14px', textAlign: 'left', borderRadius: '4px', border: '1px solid #3182ce', backgroundColor: selected === 'mnar' ? '#e53e3e' : 'transparent', color: selected === 'mnar' ? '#fff' : 'var(--ifm-font-color-base)', cursor: 'pointer', transition: '0.2s' }}>
          Option C: MNAR (Missing Not at Random)
        </button>
      </div>
      {selected === 'mcar' && <p style={{ color: '#e53e3e', marginTop: '15px', fontWeight: 'bold', fontSize: '14px', marginBottom: '0' }}>Incorrect. Missingness is not random or independent — it is explicitly triggered by an observed feature (temperature).</p>}
      {selected === 'mar' && <p style={{ color: '#38a169', marginTop: '15px', fontWeight: 'bold', fontSize: '14px', marginBottom: '0' }}>Correct! This is MAR (Missing at Random). The probability of blood pressure missing is completely explained by another observed feature (temperature). Conditioning on temperature allows multivariate models to reconstruct the signal without systematic bias.</p>}
      {selected === 'mnar' && <p style={{ color: '#e53e3e', marginTop: '15px', fontWeight: 'bold', fontSize: '14px', marginBottom: '0' }}>Incorrect. It would only be MNAR if the missingness was caused directly by the unrecorded blood pressure reading itself (e.g. pressure exceeded the physical measuring gauge).</p>}
    </div>
  );
};

<MissingDataMechanismQuiz />

### The California District Benchmark: MCAR vs. MNAR

To quantify how mechanism dictates accuracy, consider a controlled experiment on 2,000 California districts where the true median income is known ($\mu = 3.851$, $\sigma = 1.936$). Masking 30% of values under MCAR vs. MNAR reveals the operational reality:

---

export const MissingnessMechanismSimulator = () => {
  const [mechanism, setMechanism] = React.useState('mcar');

  const stats = {
    complete: { mean: 3.851, std: 1.936, corr: -0.097, desc: 'Complete ground truth across all 2,000 districts.' },
    mcar: {
      observedMean: 3.815,
      observedStd: 1.885,
      imputedMean: 3.815,
      imputedStd: 1.573,
      biasPct: '-0.9%',
      desc: '30% dropped uniformly at random. Surviving data is an unbiased representative sample.'
    },
    mnar: {
      observedMean: 2.877,
      observedStd: 0.831,
      imputedMean: 2.877,
      imputedStd: 0.696,
      biasPct: '-25.3%',
      desc: 'Top 30% highest earners hidden. The observed mean is structurally depressed.'
    }
  };

  const curr = stats[mechanism];

  return (
    <div style={{ padding: '24px', border: '1px solid var(--ifm-color-emphasis-200)', borderRadius: '8px', backgroundColor: 'rgba(148, 163, 184, 0.05)', margin: '25px 0' }}>
      <h4 style={{ margin: '0 0 12px 0' }}>Interactive Missingness Mechanism & Bias Simulator</h4>
      <p style={{ margin: '0 0 16px 0', fontSize: '14px' }}>
        Compare how removing 30% of records under MCAR vs. MNAR affects baseline statistics and mean imputation outcomes:
      </p>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '16px' }}>
        <button
          onClick={() => setMechanism('mcar')}
          style={{
            padding: '6px 14px',
            borderRadius: '4px',
            border: '1px solid #3182ce',
            backgroundColor: mechanism === 'mcar' ? '#3182ce' : 'transparent',
            color: mechanism === 'mcar' ? '#fff' : 'var(--ifm-font-color-base)',
            cursor: 'pointer',
            fontWeight: 'bold',
            fontSize: '13px',
            transition: '0.2s'
          }}
        >
          MCAR: 30% Dropped Uniformly
        </button>
        <button
          onClick={() => setMechanism('mnar')}
          style={{
            padding: '6px 14px',
            borderRadius: '4px',
            border: '1px solid #3182ce',
            backgroundColor: mechanism === 'mnar' ? '#3182ce' : 'transparent',
            color: mechanism === 'mnar' ? '#fff' : 'var(--ifm-font-color-base)',
            cursor: 'pointer',
            fontWeight: 'bold',
            fontSize: '13px',
            transition: '0.2s'
          }}
        >
          MNAR: Top 30% Incomes Hidden
        </button>
      </div>

      <div style={{ padding: '16px', borderRadius: '6px', backgroundColor: 'var(--ifm-background-surface-color)', border: '1px solid var(--ifm-color-emphasis-300)' }}>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(140px, 1fr))', gap: '10px', marginBottom: '14px' }}>
          <div style={{ padding: '10px', borderRadius: '4px', backgroundColor: 'rgba(148, 163, 184, 0.08)', textAlign: 'center' }}>
            <div style={{ fontSize: '11px', fontWeight: 'bold' }}>True Ground Truth Mean</div>
            <div style={{ fontSize: '15px', fontWeight: 'bold' }}>3.851</div>
          </div>
          <div style={{ padding: '10px', borderRadius: '4px', backgroundColor: 'rgba(148, 163, 184, 0.08)', textAlign: 'center' }}>
            <div style={{ fontSize: '11px', fontWeight: 'bold' }}>Observed Sample Mean</div>
            <div style={{ fontSize: '15px', fontWeight: 'bold', color: mechanism === 'mcar' ? '#276749' : '#c53030' }}>{curr.observedMean}</div>
          </div>
          <div style={{ padding: '10px', borderRadius: '4px', backgroundColor: 'rgba(148, 163, 184, 0.08)', textAlign: 'center' }}>
            <div style={{ fontSize: '11px', fontWeight: 'bold' }}>Mean-Imputed Mean</div>
            <div style={{ fontSize: '15px', fontWeight: 'bold', color: mechanism === 'mcar' ? '#276749' : '#c53030' }}>{curr.imputedMean}</div>
          </div>
          <div style={{ padding: '10px', borderRadius: '4px', backgroundColor: mechanism === 'mcar' ? 'rgba(72, 187, 120, 0.1)' : 'rgba(229, 62, 62, 0.1)', textAlign: 'center', border: mechanism === 'mcar' ? '1px solid #38a169' : '1px solid #e53e3e' }}>
            <div style={{ fontSize: '11px', fontWeight: 'bold' }}>Systematic Bias</div>
            <div style={{ fontSize: '15px', fontWeight: 'bold', color: mechanism === 'mcar' ? '#276749' : '#c53030' }}>{curr.biasPct}</div>
          </div>
        </div>

        <div style={{ padding: '10px 14px', borderRadius: '4px', backgroundColor: mechanism === 'mcar' ? 'rgba(72, 187, 120, 0.08)' : 'rgba(229, 62, 62, 0.08)', borderLeft: mechanism === 'mcar' ? '4px solid #38a169' : '4px solid #e53e3e', fontSize: '12px' }}>
          <b>Key Takeaway:</b> {mechanism === 'mcar' ? (
            <span>Under MCAR, surviving data is unbiased (3.815 vs 3.851). Imputing the mean preserves the central tendency accurately.</span>
          ) : (
            <span>Under MNAR, the observed mean is structurally wrong (2.877 vs 3.851). Imputing with the mean fills 600 gaps with a number that is guaranteed to be 25% too low. <b>No statistical algorithm can recover unobserved MNAR data without external assumptions.</b></span>
          )}
        </div>
      </div>
    </div>
  );
};

<MissingnessMechanismSimulator />

:::danger

**No Statistical Imputer Can Fix MNAR**

Under MNAR, advanced imputers (like KNN or Iterative MICE) cannot recover the truth because every observed training neighbor belongs to the visible portion of the distribution. When missingness is MNAR, valid engineering solutions are:
1. **Preserve the signal with `add_indicator=True`** (let the model learn from the absence of data).
2. **Collect supplementary data** targeting the missing demographic.
3. **Explicitly state the boundary limitation** of the resulting model.

:::

---

## 2. What Mean Imputation Costs: The 19% Variance Collapse

Even under valid MCAR conditions where mean imputation is statistically legitimate, replacing gaps with the column mean introduces severe mathematical distortion:

| State | Sample Mean | Standard Deviation | Variance Loss |
|---|---|---|---|
| **Complete Truth** | 3.851 | **1.936** | 0.0% |
| **MCAR (30% missing, before imputing)** | 3.815 | 1.885 | -2.6% |
| **MCAR + Mean Imputation** | 3.815 | **1.573** | **-18.8% collapse** |

### Why Variance Collapses

This collapse is pure arithmetic: substituting 600 missing values with the exact constant `3.815` means those 600 records contribute **exactly zero** squared deviation to the variance sum:

$$\sigma^2 = \frac{1}{N} \sum_{i=1}^N (x_i - \mu)^2$$

### The Three Inescapable Side Effects

1.  **Confidence Intervals Shrink Artificially:** Because $\sigma$ is understated by 19%, standard errors ($SE = \sigma / \sqrt{N}$) are too small, leading to false statistical significance ($p$-hacking by accident).
2.  **Correlations are Diluted:** A block of identical constants cannot co-vary with any other feature. Correlation coefficients between the imputed column and target variables fall monotonically.
3.  **The Distribution Develops an Artificial Spike:** The continuous density distribution develops a massive, non-existent spike (Dirac delta peak) right at the mean.

---

## 3. Listwise Deletion & The Compounding Formula $(1 - p)^k$

The simplest alternative to imputation is dropping incomplete rows: **listwise deletion** (`df.dropna()`).

While deletion invents no synthetic values, it is mathematically punishing on wide datasets. If a dataset has $k$ features, each missing a fraction $p$ independently, the fraction of surviving complete rows is:

$$\text{Share of Complete Rows Kept} = (1 - p)^k$$

---

export const DeletionCalculator = () => {
  const [cols, setCols] = React.useState(8);
  const [pct, setPct] = React.useState(10);

  const p = pct / 100;
  const kept = Math.pow(1 - p, cols);
  const dropped = 1 - kept;

  return (
    <div style={{ padding: '24px', border: '1px solid var(--ifm-color-emphasis-200)', borderRadius: '8px', backgroundColor: 'rgba(148, 163, 184, 0.05)', margin: '25px 0' }}>
      <h4 style={{ margin: '0 0 12px 0' }}>Interactive Listwise Deletion Compound Loss Calculator</h4>
      <p style={{ margin: '0 0 16px 0', fontSize: '14px' }}>
        Adjust the number of features and per-column missingness rate to observe how listwise deletion compounds exponentially across columns:
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '20px', marginBottom: '18px' }}>
        <div>
          <label style={{ fontSize: '13px', fontWeight: 'bold' }}>Number of Columns ($k$): {cols}</label>
          <input
            type="range"
            min="1"
            max="20"
            value={cols}
            onChange={(e) => setCols(parseInt(e.target.value))}
            style={{ width: '100%', marginTop: '6px' }}
          />
        </div>
        <div>
          <label style={{ fontSize: '13px', fontWeight: 'bold' }}>Missing Rate per Column ($p$): {pct}%</label>
          <input
            type="range"
            min="1"
            max="25"
            value={pct}
            onChange={(e) => setPct(parseInt(e.target.value))}
            style={{ width: '100%', marginTop: '6px' }}
          />
        </div>
      </div>

      <div style={{ padding: '16px', borderRadius: '6px', backgroundColor: 'var(--ifm-background-surface-color)', border: '1px solid var(--ifm-color-emphasis-300)' }}>
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px', textAlign: 'center', marginBottom: '12px' }}>
          <div style={{ padding: '12px', borderRadius: '4px', backgroundColor: kept > 0.6 ? 'rgba(72, 187, 120, 0.1)' : 'rgba(229, 62, 62, 0.1)', border: kept > 0.6 ? '1px solid #38a169' : '1px solid #e53e3e' }}>
            <div style={{ fontSize: '11px', fontWeight: 'bold', textTransform: 'uppercase' }}>Surviving Complete Rows</div>
            <div style={{ fontSize: '20px', fontWeight: 'bold', color: kept > 0.6 ? '#276749' : '#c53030', marginTop: '4px' }}>
              {(kept * 100).toFixed(1)}%
            </div>
          </div>
          <div style={{ padding: '12px', borderRadius: '4px', backgroundColor: dropped > 0.4 ? 'rgba(229, 62, 62, 0.1)' : 'rgba(148, 163, 184, 0.1)', border: dropped > 0.4 ? '1px solid #e53e3e' : '1px solid var(--ifm-color-emphasis-300)' }}>
            <div style={{ fontSize: '11px', fontWeight: 'bold', textTransform: 'uppercase' }}>Discarded Rows</div>
            <div style={{ fontSize: '20px', fontWeight: 'bold', color: dropped > 0.4 ? '#c53030' : 'var(--ifm-font-color-base)', marginTop: '4px' }}>
              {(dropped * 100).toFixed(1)}%
            </div>
          </div>
        </div>

        <div style={{ fontSize: '13px', color: 'var(--ifm-color-emphasis-800)', lineHeight: '1.5' }}>
          <b>The Compounding Formula:</b> (1 - {p.toFixed(2)})^{cols} = {(kept * 100).toFixed(1)}%. Notice that with only 8 columns at 10% missingness each, calling <code>df.dropna()</code> quietly discards <b>57% of your entire sample</b>!
        </div>
      </div>
    </div>
  );
};

<DeletionCalculator />

### Deletion vs. Imputation Trade-off Matrix

| Metric | Listwise Deletion (`df.dropna()`) | Statistical Imputation (`SimpleImputer`) |
|---|---|---|
| **Fabricates Data?** | **No** — preserves raw authenticity | **Yes** — substitutes point estimates |
| **Preserves Sample Size?** | **No** — loses $1 - (1-p)^k$ of rows | **Yes** — retains 100% of samples |
| **Variance Preservation** | **High** — sample spread remains natural | **Poor** — shrinks variance by substituting constants |
| **Safe Under MNAR?** | **No** — selectively removes non-reporting rows | **No** — imputes biased conditional values |
| **Recommended When** | Tiny missing fraction ($&lt;2\%$), large $N$ | Missingness spread across many columns |

---

## 4. Choosing a Strategy & The Categorical Trap

`SimpleImputer` offers four substitution strategies:

| Strategy | Replacement Value | Ideal Target Domain |
|---|---|---|
| `strategy="mean"` | Column mean ($\mu$) | Symmetric, normal numerical features with zero outliers |
| `strategy="median"` | Column median | **Skewed numerical features** or tables with extreme outliers |
| `strategy="most_frequent"` | Statistical mode | **Categorical features** or discrete ordinal integers |
| `strategy="constant"` | `fill_value` | Text fields (e.g. `"Unknown"`) or sentinel indicators |

:::warning

**The Categorical Mean Trap: Producing Impossible Numbers**

Applying `strategy="mean"` to binary or categorical columns stored as integers (`0` and `1`) produces silent nonsense:

```python
cat = pd.DataFrame({"employed": [1, 1, 0, 1, 1, np.nan, 1, 0]})
imputed = SimpleImputer(strategy="mean").fit_transform(cat)
print("Imputed value:", imputed[5, 0])
```

```text title="Output"
Imputed value: 0.714286  <- IMPOSSIBLE VALUE
```

Nobody is `71.4%` employed. The column represents a binary state, not a quantity. A scikit-learn model will consume `0.714286` without error, distorting decision boundaries. Always use `strategy="most_frequent"` on categorical columns.

:::

---

export const ImputerVarianceSandbox = () => {
  const [strategy, setStrategy] = React.useState('median');

  // Skewed data: AveOccup from housing data (mostly 2-4, one extreme 80)
  const rawData = [2.5, 3.0, 2.8, 3.2, null, 2.9, 80.0];
  const cleanData = [2.5, 3.0, 2.8, 3.2, 2.9, 80.0];
  
  const meanVal = (cleanData.reduce((a,b) => a+b, 0) / cleanData.length).toFixed(2); // 15.73
  const medianVal = 2.95;
  const modeVal = 2.8;

  const filledValue = strategy === 'mean' ? meanVal : strategy === 'median' ? medianVal : modeVal;

  return (
    <div style={{ padding: '24px', border: '1px solid var(--ifm-color-emphasis-200)', borderRadius: '8px', backgroundColor: 'rgba(148, 163, 184, 0.05)', margin: '25px 0' }}>
      <h4 style={{ margin: '0 0 12px 0' }}>Interactive Strategy Selector: Outliers & Skewness</h4>
      <p style={{ margin: '0 0 16px 0', fontSize: '14px' }}>
        Sample column containing typical values [2.5, 3.0, 2.8, 3.2, 2.9], one missing entry, and an extreme outlier [80.0]:
      </p>

      <div style={{ display: 'flex', gap: '8px', marginBottom: '16px' }}>
        {['mean', 'median', 'most_frequent'].map(s => (
          <button
            key={s}
            onClick={() => setStrategy(s)}
            style={{
              padding: '6px 14px',
              borderRadius: '4px',
              border: '1px solid #3182ce',
              backgroundColor: strategy === s ? '#3182ce' : 'transparent',
              color: strategy === s ? '#fff' : 'var(--ifm-font-color-base)',
              cursor: 'pointer',
              fontWeight: 'bold',
              fontSize: '13px',
              transition: '0.2s'
            }}
          >
            strategy="{s}"
          </button>
        ))}
      </div>

      <div style={{ padding: '16px', borderRadius: '6px', backgroundColor: 'var(--ifm-background-surface-color)', border: '1px solid var(--ifm-color-emphasis-300)' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '12px' }}>
          <div><b>Evaluated Fill Value:</b> <code style={{ fontSize: '16px', color: '#2b6cb0' }}>{filledValue}</code></div>
          <span style={{ fontSize: '12px', fontWeight: 'bold', padding: '3px 8px', borderRadius: '4px', backgroundColor: strategy === 'median' ? 'rgba(72, 187, 120, 0.15)' : 'rgba(221, 107, 32, 0.15)', color: strategy === 'median' ? '#276749' : '#c53030' }}>
            {strategy === 'median' ? 'Robust to Outliers' : strategy === 'mean' ? 'Heavily Distorted' : 'Mode Fallback'}
          </span>
        </div>

        <div style={{ fontSize: '13px', color: 'var(--ifm-color-emphasis-800)', lineHeight: '1.5' }}>
          {strategy === 'mean' && (
            <span>The outlier <code>80.0</code> inflates the mean to <code>15.73</code>. Filling the gap with 15.73 imputes an artificial value 5x larger than typical neighborhood occupancies.</span>
          )}
          {strategy === 'median' && (
            <span>The median centers at <code>2.95</code>, completely ignoring the extreme outlier and substituting a realistic value matching typical neighborhood occupancies.</span>
          )}
          {strategy === 'most_frequent' && (
            <span>The mode selects <code>2.8</code> (the most common value). Useful for categorical data, but less efficient for continuous measurements.</span>
          )}
        </div>
      </div>
    </div>
  );
};

<ImputerVarianceSandbox />

### Preserving Information with `add_indicator=True`

If data is missing under MAR or MNAR, *the fact that a value was missing* is itself an informative feature.

By default, imputation destroys this signal. Passing `add_indicator=True` to `SimpleImputer` instructs scikit-learn to append binary indicator columns:

```python
imp = SimpleImputer(strategy="median", add_indicator=True)
X_imputed = imp.fit_transform(X)
```

```text title="Output Matrix"
 age  salary  age_missing  salary_missing
25.0 50000.0          0.0             0.0
30.0 72500.0          0.0             1.0  <- Salary was missing
35.0 70000.0          0.0             0.0
32.5 80000.0          1.0             0.0  <- Age was missing
```

Downstream algorithms can now learn patterns like *"clients with missing salary have a 3x higher default risk"*, preserving predictive utility even if the imputed point estimate is imperfect.

---

## 5. When Smarter Imputers Pay Off: KNN vs. Iterative (MICE)

While `SimpleImputer` treats each column in total isolation, multivariate imputers utilize correlations across features:

*   **`KNNImputer`:** Finds the $k$ most similar rows based on Euclidean distance across observed columns, and calculates a weighted average.
*   **`IterativeImputer` (MICE):** Models each feature as a regression function of all other features in round-robin fashion until convergence.

### The Correlation Rule: Sophistication Requires Signal

In empirical trials masking 20% of values, recovery error (where `0.0` = perfect, `1.0` = guessing the mean) reveals an essential rule:

| Feature Predictability | `SimpleImputer(mean)` | `KNNImputer(k=5)` | `IterativeImputer` | Outcome |
|---|---|---|---|---|
| **High Correlation** ($r = 0.83$, Bedrooms vs. Rooms) | 1.475 | **0.725** | 0.727 | **KNN cuts error in half (51% improvement)** |
| **Zero Correlation** ($r = 0.03$, House Age vs. Occupancy) | **0.984** | 1.056 | 0.984 | **KNN is WORSE than guessing the mean!** |

:::tip

**Always Inspect Correlation Before Reaching for Complex Imputers:**

```python
df.corr()[column_with_gaps].abs().sort_values(ascending=False)
```

If the highest inter-feature correlation is weak ($r &lt; 0.3$), `KNNImputer` averages neighbors across irrelevant dimensional space, injecting noise. **Multivariate imputers pay off in direct proportion to feature correlation, and not otherwise.**

:::

---

export const SmartImputerAdvisor = () => {
  const [corr, setCorr] = React.useState(0.8);
  const [skew, setSkew] = React.useState('normal');

  let recommendation = 'SimpleImputer(strategy="median")';
  let badgeColor = '#2b6cb0';
  let rationale = '';

  if (corr < 0.35) {
    recommendation = 'SimpleImputer(strategy="median")';
    badgeColor = '#3182ce';
    rationale = 'Inter-feature correlation is too weak for multivariate modeling. KNNImputer would average irrelevant neighbors and perform worse than simple point estimates.';
  } else if (skew === 'heavy') {
    recommendation = 'KNNImputer(n_neighbors=5)';
    badgeColor = '#d69e2e';
    rationale = 'Strong correlation exists, but heavy-tailed skewness causes IterativeImputer (linear regression) to extrapolate wild predictions. KNNImputer is bounded and safer.';
  } else {
    recommendation = 'IterativeImputer (MICE) or KNNImputer';
    badgeColor = '#38a169';
    rationale = 'Strong linear correlation and normal distribution make multivariate regression highly effective, cutting recovery error by up to 50% compared to mean substitution.';
  }

  return (
    <div style={{ padding: '24px', border: '1px solid var(--ifm-color-emphasis-200)', borderRadius: '8px', backgroundColor: 'rgba(148, 163, 184, 0.05)', margin: '25px 0' }}>
      <h4 style={{ margin: '0 0 12px 0' }}>Interactive Smart Imputer Decision Advisor</h4>
      <p style={{ margin: '0 0 16px 0', fontSize: '14px' }}>
        Tune the observed inter-feature correlation and distribution shape to determine the optimal imputation strategy:
      </p>

      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '20px', marginBottom: '18px' }}>
        <div>
          <label style={{ fontSize: '13px', fontWeight: 'bold' }}>Max Inter-Feature Correlation ($r$): {corr.toFixed(2)}</label>
          <input
            type="range"
            min="0"
            max="100"
            value={corr * 100}
            onChange={(e) => setCorr(parseInt(e.target.value) / 100)}
            style={{ width: '100%', marginTop: '6px' }}
          />
        </div>
        <div>
          <label style={{ fontSize: '13px', fontWeight: 'bold' }}>Column Distribution Shape</label>
          <div style={{ display: 'flex', gap: '8px', marginTop: '6px' }}>
            <button
              onClick={() => setSkew('normal')}
              style={{
                padding: '6px 12px',
                borderRadius: '4px',
                border: '1px solid #3182ce',
                backgroundColor: skew === 'normal' ? '#3182ce' : 'transparent',
                color: skew === 'normal' ? '#fff' : 'var(--ifm-font-color-base)',
                cursor: 'pointer',
                fontSize: '12px'
              }}
            >
              Roughly Symmetric
            </button>
            <button
              onClick={() => setSkew('heavy')}
              style={{
                padding: '6px 12px',
                borderRadius: '4px',
                border: '1px solid #3182ce',
                backgroundColor: skew === 'heavy' ? '#3182ce' : 'transparent',
                color: skew === 'heavy' ? '#fff' : 'var(--ifm-font-color-base)',
                cursor: 'pointer',
                fontSize: '12px'
              }}
            >
              Heavy-Tailed Skew
            </button>
          </div>
        </div>
      </div>

      <div style={{ padding: '16px', borderRadius: '6px', backgroundColor: 'var(--ifm-background-surface-color)', border: '1px solid var(--ifm-color-emphasis-300)' }}>
        <div style={{ fontSize: '12px', textTransform: 'uppercase', fontWeight: 'bold', color: 'var(--ifm-color-emphasis-600)', marginBottom: '4px' }}>
          Recommended Architecture
        </div>
        <div style={{ fontSize: '16px', fontWeight: 'bold', color: badgeColor, marginBottom: '8px' }}>
          {recommendation}
        </div>
        <div style={{ fontSize: '13px', color: 'var(--ifm-color-emphasis-800)', lineHeight: '1.5' }}>
          {rationale}
        </div>
      </div>
    </div>
  );
};

<SmartImputerAdvisor />

---

<Heading as="h2" id="the-fit--transform-contract">6. The fit / transform Contract & Preventing Data Leakage</Heading>

Imputation is the foundational demonstration of scikit-learn's cardinal rule: **`fit` learns, `transform` applies**.

```python
# 1. Fit computes statistics strictly on the training partition
imputer = SimpleImputer(strategy="median")
imputer.fit(X_train)

# 2. Transform applies those frozen training statistics to both partitions
X_train_imputed = imputer.transform(X_train)
X_test_imputed  = imputer.transform(X_test)  # NEVER call fit or fit_transform here!
```

If you call `fit_transform` on `X_test`, the imputer computes test-set medians, leaking testing bounds into validation features. Test records must be imputed using **training statistics**, mimicking live inference where future test outcomes are completely unknown.

### The All-NaN Silent Column Drop Trap

If a column in your training fold happens to be 100% missing, `SimpleImputer` cannot compute a mean or median. By default, it **silently drops the column**, returning an output matrix with fewer columns than input:

```python
bad_df = pd.DataFrame({"a": [1.0, 2.0, 3.0], "b": [np.nan, np.nan, np.nan]})
out = SimpleImputer(strategy="mean").fit_transform(bad_df)
print("Output shape:", out.shape)  # Returns (3, 1) — column 'b' has VANISHED!
```

If column `b` is present in the test set, downstream models will crash with dimensional mismatch exceptions. To prevent this, pass **`keep_empty_features=True`** in scikit-learn 1.2+ to retain the column populated with zeros.

---

## 7. Implementation Lab

:::tip

**Implementation Lab: Missing Value Imputation Benchmarks**

Trace how MCAR vs. MNAR missingness alters baseline statistical distributions, calculate listwise deletion row volumes across scaling dimensions, and evaluate MAE performances of Simple, KNN, and Iterative (MICE) imputers live on the California housing dataset.

<a href="https://colab.research.google.com/github/vettrivel-kl/vettrivel-kl.github.io/blob/master/static/labs/02-data-preprocessing/imputation_lab.ipynb" target="_blank" rel="noopener noreferrer">
  <img src="https://colab.research.google.com/assets/colab-badge.svg" alt="Open In Colab" style={{ height: '48px', width: 'auto', display: 'block', margin: '15px 0' }} />
</a>

**How to run the lab:**
1. Click the **"Open In Colab"** badge above to launch the interactive notebook.
2. Click **"Runtime" > "Run all"** or execute cells individually using `Shift + Enter`.
3. Follow the guided experiments: test the 19% variance loss, benchmark KNN vs. MICE recovery error, and inspect `add_indicator=True` feature matrices.

:::

### Comparative Implementation: Baseline vs. Multivariate Imputation

<Tabs>
  <TabItem value="simple" label="1. SimpleImputer with MissingIndicator" default>
    <br/>

    ```python
    from sklearn.impute import SimpleImputer
    from sklearn.pipeline import Pipeline
    from sklearn.linear_model import LogisticRegression

    # Median imputation robust to outliers + binary indicator flag for MAR/MNAR
    pipe = Pipeline([
        ("imputer", SimpleImputer(strategy="median", add_indicator=True)),
        ("classifier", LogisticRegression(max_iter=1000))
    ])

    pipe.fit(X_train, y_train)
    score = pipe.score(X_test, y_test)
    ```
  </TabItem>
  <TabItem value="knn" label="2. Multivariate KNNImputer">
    <br/>

    ```python
    from sklearn.impute import KNNImputer
    from sklearn.preprocessing import StandardScaler
    from sklearn.pipeline import Pipeline
    from sklearn.linear_model import LogisticRegression

    # Distance-weighted nearest neighbors imputation (requires scaling!)
    pipe = Pipeline([
        ("scaler", StandardScaler()),
        ("imputer", KNNImputer(n_neighbors=5)),
        ("classifier", LogisticRegression(max_iter=1000))
    ])

    pipe.fit(X_train, y_train)
    score = pipe.score(X_test, y_test)
    ```
  </TabItem>
</Tabs>

---

## 8. Common Mistakes

<Tabs>
  <TabItem value="m1" label="1. Imputing before asking WHY" default>
    <br/>

    *   **The Misconception:** *"Our dataset has missing values, so let's call SimpleImputer(strategy='mean') and proceed."*
    *   **Wrong Thinking:** Assuming all missing data can be solved with substitution.
    *   **The Right Principle:** Diagnose whether data gaps are **MCAR**, **MAR**, or **MNAR** first. Under MNAR, the observed mean is already biased by 25%. Mean substitution produces a confidently wrong model. Use `add_indicator=True` to let the model learn from missingness.
  </TabItem>
  <TabItem value="m2" label="2. Scaling Before Imputing">
    <br/>

    *   **The Misconception:** *"Standardize features first, then impute missing values."*
    *   **Wrong Thinking:** Calling `StandardScaler().fit_transform(X)` on matrices containing NaNs.
    *   **The Right Principle:** Always impute first, scale second. Standardizing standard deviations is corrupted by missing coordinates, and many scalers fail on NaN entries.
  </TabItem>
  <TabItem value="m3" label="3. Blind Listwise Deletion">
    <br/>

    *   **The Misconception:** *"Only 10% of values are missing per column, so calling df.dropna() is harmless."*
    *   **Wrong Thinking:** Forgetting that row loss compounds exponentially across columns via $(1 - p)^k$.
    *   **The Right Principle:** On an 8-column dataset with 10% missingness per column, calling `dropna()` discards **57% of your data**. Use imputation rather than discarding clean features from partially missing rows.
  </TabItem>
  <TabItem value="m4" label="4. fit_transform on Test Data">
    <br/>

    *   **The Misconception:** *"Call imputer.fit_transform(X_test) so the test set is filled using its own mean."*
    *   **Wrong Thinking:** Re-calculating substitution statistics on the validation partition.
    *   **The Right Principle:** Imputers must learn substitution statistics strictly on `X_train`. The test set must be transformed using training statistics via `imputer.transform(X_test)` to prevent data leakage.
  </TabItem>
</Tabs>

---

## 9. Summary

<div className="row">
  <div className="col col--6">
    <div className="card" style={{ height: '100%', marginBottom: '15px' }}>
      <div className="card__header" style={{ padding: '15px', borderBottom: '1px solid var(--ifm-color-emphasis-200)' }}>
        <h3>Statistical Foundations</h3>
      </div>
      <div className="card__body" style={{ padding: '15px' }}>
        <ul>
          <li><b>Rubin's Taxonomy:</b> MCAR (recoverable), MAR (multivariate recoverable), MNAR (unrecoverable bias).</li>
          <li><b>The Variance Collapse:</b> Mean substitution replaces points with constants, reducing standard deviation by ~19%.</li>
          <li><b>Deletion Compounding:</b> Listwise deletion keeps $(1 - p)^k$ rows; 8 columns at 10% missingness loses 57% of data.</li>
        </ul>
      </div>
    </div>
  </div>
  <div className="col col--6">
    <div className="card" style={{ height: '100%', marginBottom: '15px' }}>
      <div className="card__header" style={{ padding: '15px', borderBottom: '1px solid var(--ifm-color-emphasis-200)' }}>
        <h3>Operational Imputation Rules</h3>
      </div>
      <div className="card__body" style={{ padding: '15px' }}>
        <ul>
          <li><b>Strategy Selection:</b> Use <code>median</code> for skewed metrics; use <code>most_frequent</code> for binary/categorical fields.</li>
          <li><b>Correlation Prerequisite:</b> `KNNImputer` requires $r &gt; 0.3$; otherwise it performs worse than guessing the mean.</li>
          <li><b>Preserve Signal:</b> Use <code>add_indicator=True</code> to retain missingness patterns under MAR/MNAR.</li>
          <li><b>Test Isolation:</b> Fit imputers on `X_train`; apply to `X_test` via `.transform()`.</li>
        </ul>
      </div>
    </div>
  </div>
</div>

<br/>

:::info

**Key Takeaways**

*   **No Imputer Fixes MNAR:** Imputing with the observed mean under MNAR cements a 25% bias. Preserve the signal with `add_indicator=True`.
*   **Mean Imputation Distorts Variance:** Replaces diverse observations with identical constants, artificially shrinking confidence intervals by ~19%.
*   **The Categorical Mean Trap:** Mean substitution on binary integer columns produces impossible values like `0.714286`. Always use `most_frequent`.
*   **Fit on Train, Transform on Test:** Never call `fit_transform` on test data. Wrap imputers and models in a unified `Pipeline` to enforce test isolation.

:::

**Next in this section:** [Encoding Categorical Data](./03-encoding-categorical-data.mdx) - transforming strings and categories into model-ready numbers.

**See also:** [Loading and Preparing Data](./01-loading-and-preparing-data.mdx) · [The Toolkit and the Pipeline](../01-foundations/05-toolkit-and-pipeline.mdx)

---

## 10. Active Recall Flashcards

*Attempt each conceptual diagnostic question first, then click to reveal the underlying derivation and explanation.*

<details>
  <summary><b>1. [THEORY] Define Donald Rubin's three missing data mechanisms (MCAR, MAR, MNAR) and state whether statistical imputation can recover the true mean in each.</b></summary>
  <div className="answer-content">
    <br/>

    *   **MCAR (Missing Completely at Random):** Missingness is completely independent of both observed and unobserved data ($P(M \mid Y_{obs}, Y_{mis}) = P(M)$). The surviving sample is unbiased. **Imputation can recover the true mean.**
    *   **MAR (Missing at Random):** Missingness depends on other observed features in the table, but not the missing variable itself ($P(M \mid Y_{obs}, Y_{mis}) = P(M \mid Y_{obs})$). **Multivariate imputation (KNN / MICE) can recover the mean** by conditioning on those observed columns.
    *   **MNAR (Missing Not at Random):** Missingness depends directly on the unobserved missing value itself ($P(M \mid Y_{obs}, Y_{mis}) \neq P(M \mid Y_{obs})$). The observed distribution is systematically biased. **No statistical imputer can recover the true mean.**
  </div>
</details>

<details>
  <summary><b>2. [ANALYZE] In the California district income benchmark, why did applying mean imputation under MNAR produce an unrecoverable 25% bias?</b></summary>
  <div className="answer-content">
    <br/>

    Under MNAR, the top 30% of highest incomes were hidden. The observed mean of the remaining 70% was `2.877` against the true population mean of `3.851`. Mean substitution computes the average over observed data, filling all 600 missing gaps with `2.877`. Every imputed value is systematically too low, locking in an unrecoverable 25.3% downward bias.
  </div>
</details>

<details>
  <summary><b>3. [THEORY] Why does mean imputation cause standard deviation to collapse by ~19% under MCAR? What operational risks does this introduce?</b></summary>
  <div className="answer-content">
    <br/>

    *   **Why It Collapses:** Substituting 600 missing entries with a single constant value ($\mu$) means those 600 entries contribute exactly zero squared deviation ($(x_i - \mu)^2 = 0$) to the variance formula, artificially deflating standard deviation from `1.936` to `1.573`.
    *   **Operational Risks:** Understated variance leads to artificially narrow confidence intervals and inflated $t$-statistics, creating false statistical significance. Furthermore, inter-feature correlations are diluted because constants cannot co-vary.
  </div>
</details>

<details>
  <summary><b>4. [THEORY] State the listwise deletion compounding formula for $k$ features with missing rate $p$. What percentage of rows is discarded on an 8-column dataset with 10% missingness per column?</b></summary>
  <div className="answer-content">
    <br/>

    *   **Compounding Formula:** $\text{Share of complete rows retained} = (1 - p)^k$.
    *   **Calculation for $k=8, p=0.10$:**
        $$(1 - 0.10)^8 = 0.90^8 \approx 0.4304 \quad (43.0\% \text{ kept})$$
    *   **Percentage Discarded:** Exactly $100\% - 43.0\% = \mathbf{57.0\%}$ of all rows are discarded. Listwise deletion penalizes wide tables exponentially.
  </div>
</details>

<details>
  <summary><b>5. [ANALYZE] A junior engineer runs `SimpleImputer(strategy='mean')` on a binary column `employed: [1, 0]`. What value is imputed, why is it dangerous, and what should be used instead?</b></summary>
  <div className="answer-content">
    <br/>

    *   **Imputed Value:** The mean of the binary indicators, resulting in a fractional value like `0.714286`.
    *   **Why Dangerous:** `0.714286` is a non-existent state (nobody is 71.4% employed). The matrix remains numeric so no error is raised, but the fractional value distorts decision boundaries and linear weights.
    *   **Correct Strategy:** Use `strategy="most_frequent"` (mode imputation), which guarantees that filled values match valid categorical states.
  </div>
</details>

<details>
  <summary><b>6. [THEORY] Under what mathematical condition does `KNNImputer` outperform `SimpleImputer`? What happens when this condition is violated?</b></summary>
  <div className="answer-content">
    <br/>

    *   **Required Condition:** High correlation between the missing feature and surrounding columns ($r &gt; 0.35$).
    *   **When Violated ($r \to 0$):** In the California benchmark on an uncorrelated feature ($r = 0.03$), `KNNImputer` achieved an error of `1.0566` — **worse than guessing the mean** (`0.9840`). When features do not co-vary, KNN averages neighbors across irrelevant metric space, injecting distance noise.
  </div>
</details>

<details>
  <summary><b>7. [ANALYZE] Why is `add_indicator=True` in `SimpleImputer` considered "the closest thing to a free win" under MAR or MNAR?</b></summary>
  <div className="answer-content">
    <br/>

    Under MAR and MNAR, whether a patient or customer declined to provide an answer is itself an informative predictor. Standard imputation fills the gap but destroys the missingness signal. `add_indicator=True` appends a binary column (`1.0` if missing, `0.0` if observed), allowing downstream models to learn patterns from the missingness itself even if the imputed point estimate is imperfect.
  </div>
</details>

<details>
  <summary><b>8. [THEORY] Explain the scikit-learn `fit` / `transform` contract for imputers. What error occurs if `fit_transform` is called on `X_test`?</b></summary>
  <div className="answer-content">
    <br/>

    *   **The Contract:** `.fit()` computes substitution metrics ($\mu, \text{median}$) strictly from training rows `X_train`. `.transform()` substitutes those frozen training statistics into missing cells on both `X_train` and `X_test`.
    *   **Error on Test:** Calling `fit_transform(X_test)` recomputes medians over test rows, causing **Data Leakage**. The test set influences its own preprocessing, artificially inflating validation scores.
  </div>
</details>

<details>
  <summary><b>9. [ANALYZE] Describe the all-NaN column trap in `SimpleImputer`. How does it cause silent downstream pipeline crashes, and how is it resolved?</b></summary>
  <div className="answer-content">
    <br/>

    *   **The Trap:** If a feature column in `X_train` happens to be 100% missing, `SimpleImputer` cannot compute a mean or median. By default, it **silently drops the column**, returning a matrix with shape $(N, D-1)$.
    *   **Downstream Crash:** If that column is present in `X_test`, `imputer.transform(X_test)` or the downstream model will throw a shape mismatch error because the training pipeline dropped a column.
    *   **Resolution:** Pass `keep_empty_features=True` to `SimpleImputer` (introduced in scikit-learn 1.2+), which retains the empty column populated with zeros.
  </div>
</details>

<details>
  <summary><b>10. [PROG] Write a leak-free scikit-learn pipeline chaining median imputation with missing indicator flags and a LogisticRegression classifier.</b></summary>
  <div className="answer-content">
    <br/>

    ```python
    from sklearn.datasets import load_breast_cancer
    from sklearn.model_selection import train_test_split
    from sklearn.impute import SimpleImputer
    from sklearn.preprocessing import StandardScaler
    from sklearn.linear_model import LogisticRegression
    from sklearn.pipeline import Pipeline

    # 1. Load & 2. Split
    data = load_breast_cancer()
    X_train, X_test, y_train, y_test = train_test_split(
        data.data, data.target, test_size=0.2, random_state=42, stratify=data.target
    )

    # 3. Build Unified Pipeline (Impute -> Scale -> Classify)
    pipeline = Pipeline([
        ("imputer", SimpleImputer(strategy="median", add_indicator=True)),
        ("scaler", StandardScaler()),
        ("model", LogisticRegression(max_iter=1000))
    ])

    # 4. Fit & 5/6. Evaluate
    pipeline.fit(X_train, y_train)
    print(f"Leak-Free Test Accuracy: {pipeline.score(X_test, y_test):.4f}")
    ```
  </div>
</details>
